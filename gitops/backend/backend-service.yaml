apiVersion: v1
kind: Service

metadata:
  name: backend-service
  namespace: devops-project

  labels:
    app: backend

spec:
  selector:
    app: backend

  ports:
  - name: http
    protocol: TCP
    port: 80
    targetPort: 5000

  type: ClusterIP